[package]
org = "opto-sync"
name = "opto-sync-monorepo"
version = "0.1.0"
description = "Immutable Opto Sync component composition and provenance verifier"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/opto-sync/opto-sync-monorepo"

[targets.node]
dir = "."
adapter = "node"

[publish]
enabled = false
